import * as React from 'react'

interface ICardProps {
}

const Card: React.FunctionComponent<ICardProps> = (props) => {
    return (
        <>
            Card
        </>
    )
}

export default Card
